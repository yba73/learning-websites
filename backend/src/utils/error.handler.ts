// import { Response } from "express";
// export const errorHandler = (message: string, error: any, res: Response) => {
//   res.status(500).json({ status: "fail", message: "something wrong" });
//   throw new Error(`${message} ${error}`);
// };

